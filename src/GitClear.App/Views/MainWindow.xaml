<Window x:Class="GitClear.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GitClear.App.ViewModels"
        xmlns:behaviors="clr-namespace:GitClear.App.Behaviors"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="{Binding Title}" Height="700" Width="1180"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: folder toolbar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Folder:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Grid.Column="1" VerticalAlignment="Center" Padding="4,3"
                     Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Content="Browse…" Margin="8,0,0,0" Padding="12,4"
                    Command="{Binding BrowseForFolderCommand}" />
            <Button Grid.Column="3" Content="Find repositories" Margin="8,0,0,0" Padding="12,4"
                    Command="{Binding FindRepositoriesCommand}" />
            <Button Grid.Column="4" Content="Stop" Margin="8,0,0,0" Padding="12,4"
                    Command="{Binding StopCommand}" />
        </Grid>

        <!-- Row 1: unified busy indicator. Uses Hidden (not Collapsed) when idle so
             the row keeps its height and the controls below never shift. -->
        <ProgressBar Grid.Row="1" Height="3" Margin="0,8,0,0" IsIndeterminate="True">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Hidden" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>

        <!-- Row 2: three panes — repositories | folders | files -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="160" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" MinWidth="220" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Repositories">
                <ListBox ItemsSource="{Binding Repositories}"
                         SelectedItem="{Binding SelectedRepository}"
                         HorizontalContentAlignment="Stretch"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2" ToolTip="{Binding FullPath}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <TextBlock Text="(worktree)" Margin="6,0,0,0" Opacity="0.6"
                                               Visibility="{Binding IsWorktreeOrSubmodule, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                                <TextBlock Text="{Binding FullPath}" Opacity="0.7" FontSize="11"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="Transparent" />

            <GroupBox Grid.Column="2" Header="Folders">
                <TreeView ItemsSource="{Binding RootNodes}"
                          behaviors:TreeViewSelectedItemBehavior.SelectedItem="{Binding SelectedFolder, Mode=TwoWay}"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vm:FolderNodeViewModel}"
                                                  ItemsSource="{Binding Subfolders}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" IsThreeState="False"
                                          VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                <TextBlock Text="🗀" Margin="6,0,0,0" Opacity="0.7" VerticalAlignment="Center"
                                           ToolTip="Entire folder is ignored — deleted as a unit"
                                           Visibility="{Binding IsFullyIgnored, Converter={StaticResource BoolToVis}}" />
                                <TextBlock Text="{Binding FormattedSize}" Opacity="0.55" Margin="8,0,0,0"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="{Binding TotalFileCount, StringFormat=({0} files)}"
                                           Opacity="0.4" Margin="6,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <GridSplitter Grid.Column="3" Width="6" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="Transparent" />

            <GroupBox Grid.Column="4" Header="Files in selected folder">
                <ListView ItemsSource="{Binding SelectedFolder.Files}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="34">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                  IsThreeState="False" HorizontalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Name" Width="340"
                                            DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Size" Width="120"
                                            DisplayMemberBinding="{Binding FormattedSize}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>

        <!-- Row 3: selection summary / actions -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold"
                       Text="{Binding Selection.Summary}" />
            <Button Grid.Column="1" Content="Undo last delete" Padding="14,6" Margin="0,0,8,0"
                    Command="{Binding UndoCommand}" />
            <Button Grid.Column="2" Content="Delete selected → Recycle Bin" Padding="14,6"
                    Command="{Binding DeleteSelectedCommand}" />
        </Grid>

        <!-- Row 4: status bar -->
        <StatusBar Grid.Row="4" Margin="0,8,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
